describe('something', function() {
  it('should have some specific behavior that takes lots of words to explain ' +
     'so we wrap to the next line', () => {
    // We expect indentation to be here
    expect(true);
  });
});
